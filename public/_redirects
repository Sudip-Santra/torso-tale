# Netlify redirects for SPA routing
# This ensures all routes are handled by the React app

# Handle all routes by serving index.html
/*    /index.html   200

# Optional: Handle specific file types that should be served directly
# (These are already handled correctly, but listed for clarity)
/*.js     /js/:splat     200
/*.css    /css/:splat    200
/*.png    /images/:splat 200
/*.jpg    /images/:splat 200
/*.jpeg   /images/:splat 200
/*.gif    /images/:splat 200
/*.webp   /images/:splat 200
/*.svg    /images/:splat 200